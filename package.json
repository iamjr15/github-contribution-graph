{
  "name": "github-contribution-graph-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "GitHub Contribution Graph Widget - Monorepo",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "wrangler pages dev cf-dist --compatibility-date=2026-04-02",
    "build": "npm run build -w packages/github-contribution-graph && bash scripts/build-cf-dist.sh",
    "build:cf": "bash scripts/build-cf-dist.sh",
    "test": "npm run test -w packages/github-contribution-graph",
    "test:coverage": "npm run test:coverage -w packages/github-contribution-graph"
  },
  "dependencies": {
    "@octokit/graphql": "^7.0.2"
  },
  "license": "Apache-2.0",
  "engines": {
    "node": ">=18.0.0"
  }
}
